import { RemoveAttributeDirective } from './remove-attribute.directive';

describe('RemoveAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
